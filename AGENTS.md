# Agent Instructions

Git, `gh`, Docker, and Docker Compose are installed. Do not install anything else on the host. Instead, create and use a Dev Container with all tools needed for running, debugging, and testing.
