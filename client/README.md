# Ito WebXR client

A small vanilla JavaScript WebXR client. Run `npm install && npm run dev` for
development, or use the root Compose application for HTTPS and signaling
proxies. In-headset blocks use controller-tip contact and debounce.
